services:
  - type: web
    name: chip-tracker
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn server:app --host 0.0.0.0 --port $PORT
    disk:
      name: chip-data
      mountPath: /opt/render/project/src/chip_data
      sizeGB: 1
